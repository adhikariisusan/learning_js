// Write a function isEven(num) that takes a number as an argument and returns true if it's even and false if it's odd.

function isEven(num)
{
    if(num%2==0)
        return true;
    return false;
}

console.log(isEven(7))